<%= form_for ([@todo_list, @todo_list.todo_items.find(params[:id])])  do |f| %>
    <%= f.text_field :content, placeholder: "Add a Todo Item and hit enter" , class:"form-control todo_item", required: true%></br>
        <%= f.submit "Submit", class:"btn btn-primary"%>
<% end %>
