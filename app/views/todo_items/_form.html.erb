  <%= form_for([@todo_list,@todo_list.todo_items.build])  do |f| %>
        <%= f.text_field :content, placeholder: "Add a Todo Item and hit enter" , autofocus: "on", class:"form-control todo_item", required: true%></br>
        <%= f.submit "Submit", class:"btn btn-primary"%>
    <% end %>
